export function ReportWorkspace() {
  return (
    <section className="panel report-workspace">
      <h1>Live Signals And Reports</h1>
      <p>
        React rewrite target for live anomaly cards, provider selector, signal form, report preview, full report, diagnostics, and settlement panel.
      </p>
      <div className="empty-state">No signal selected yet.</div>
    </section>
  );
}
