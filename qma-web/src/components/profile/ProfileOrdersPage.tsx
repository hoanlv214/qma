export function ProfileOrdersPage() {
  return (
    <main className="page-grid">
      <section className="panel">
        <h1>Profile Orders</h1>
        <p>Port `user.html` + `public/user.js`: wallet summary, paid reports, payment rows, local wallet events, and lazy report detail.</p>
      </section>
    </main>
  );
}
